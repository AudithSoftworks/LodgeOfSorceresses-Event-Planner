<!DOCTYPE html>
{% set locale = app.make('translator').getLocale() %}
<html lang="{{ locale }}">
    <head>
        {{ block('document_head') }}
    </head>
    <body id="root" class="theme-default" data-error-message="{{ session_get('errors') }}" />
</html>
